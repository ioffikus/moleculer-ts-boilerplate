/*
import { Context } from 'koa';

export const setUserInfoToState = async (ctx: Context, next: Function) => {
  ctx.state.userInfo = {
    ip: ctx.request.ip,
    os: ctx.userAgent.os,
    browser: ctx.userAgent.browser,
    source: ctx.userAgent.source,
  };

  return next();
};
*/
